# Frontend
This is a minimal React (Vite) frontend for the Task Manager.

Install and run:

```bash
cd taskmanager/frontend
npm install
npm run dev
```

Edit API base URL in `.env` or set `VITE_API_BASE_URL` to point to the backend.
